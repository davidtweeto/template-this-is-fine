/** Frames for each text line to slide in from the right. */
export const BASE_ENTER = 8;

/** Frames for each text line to slide out to the left. */
export const BASE_EXIT = 7;

/** Frames the camera shakes when a line lands. */
export const SHAKE_DURATION = 22;

export const FPS = 30;
